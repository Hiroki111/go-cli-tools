# todo-server

# Example usage

```bash
go build

# If /tmp/testtodoclient01.json doesn't exist and a client requests to add a new todo, todo-server will create this file
./todo-server -f /tmp/testtodoclient01.json
```